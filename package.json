{
  "name": "black-shores-agent",
  "version": "0.6.0",
  "private": true,
  "description": "Local-first organizational runtime for the BLACK_SHORES_AGENT MVP",
  "scripts": {
    "setup": "node setup.js",
    "start": "node server.js",
    "test": "node --test organization-core.test.js server.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
